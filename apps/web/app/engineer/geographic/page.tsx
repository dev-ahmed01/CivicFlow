import { EngineerProjectList } from "../_components/project-list";

export default function GeographicProjectsPage() {
  return <EngineerProjectList scope="geographic" title="Geographic Projects" description="Coordinate with area work across agencies. Non-owned projects are read-only." />;
}
